# Vercel's Python runtime installs this automatically. It is the *web demo's*
# dependency list, not the library's -- `pyproject.toml` is the source of truth
# for what `pip install docx-redline` pulls in, and `requirements-lock.txt` is
# the fully-pinned export of that.
#
# docx_redline itself is imported from the repository (api/index.py puts the
# root on sys.path), so only its runtime dependencies are listed here.
fastapi==0.115.6
jinja2==3.1.5

# docx_redline's own runtime dependencies
lxml==6.1.2
python-docx==1.2.0
python-dotenv==1.2.3
