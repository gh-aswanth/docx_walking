# Vercel's Python builder installs with `uv sync` from uv.lock, so the lock,
# pyproject.toml and README.md (the readme hook reads it) must all stay.
#
# Everything below is never served. `.mypy_cache` alone was 48 MB of a 49 MB
# upload before this file listed it.
.git/
.github/
.idea/
.venv/
.vercel/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.cache/
__pycache__/
tests/
scripts/
docs/
dist/
examples/output/
requirements-lock.txt
